#!/usr/bin/env bash

# Work in progress

# -- latest (-l)
# Downloads latest XNU tarbal into the tarballs/ directory

# --release (-r)
# Downloads the specified release tarball into a directory named after the version in the tarballs/ directory

# -- extract (-e)
# Extracts the tarball from either -l or -r into a folder named after its version in the sources/ directory

# -- sync
# Synchronises the sourcecode to the url set in the REPO_URL directory

